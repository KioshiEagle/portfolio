---
import ProjectCard from '../components/ProjectCard.astro';
import { MY_PROJECTS } from '../types';
---

<html lang="fr">
	<head>
		<meta charset="utf-8" />
		<title>Jean-Charles Vente | Portfolio Ingénieur</title>
		<style>
			body { font-family: system-ui, sans-serif; background: #0f172a; color: white; margin: 0; padding: 4rem 2rem; }
			.container { max-width: 900px; margin: 0 auto; }
			.grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; margin-top: 3rem; }
			h1 { font-size: 3rem; margin-bottom: 1rem; }
			.intro { font-size: 1.25rem; color: #94a3b8; max-width: 600px; }
		</style>
	</head>
	<body>
		<div class="container">
			<h1>Jean-Charles Vente</h1>
			<p class="intro">Étudiant en 2ème année à <strong>Télécom Nancy</strong>. Futur ingénieur logiciel passionné par le code propre et les systèmes performants.</p>
			
			<div class="grid">
				{MY_PROJECTS.map(p => <ProjectCard project={p} />)}
			</div>
		</div>
	</body>
</html>